# Copy to agent/.env (gitignored, never committed) and fill in the real PIN.
# Picked up automatically by reef-agent.mjs — nothing to pass on the command
# line, so it never sits in shell history or a `ps aux` listing.
REEF_PIN=
